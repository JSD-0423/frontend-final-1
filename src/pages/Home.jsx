import { Box } from "@mui/material";
import React from "react";

import { HeroSection } from "../components";

const Home = () => {
  return (
    <Box sx={{ padding: "20px" }}>
      <HeroSection />
    </Box>
  );
};

export default Home;
